apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: jibaek-woodoku-app-ingress
  namespace: jibaek
spec:
  ingressClassName: gce
  rules:
  - host: "woodoku.baekjaein.com"
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: jibaek-woodoku-app-service
            port:
              number: 8080